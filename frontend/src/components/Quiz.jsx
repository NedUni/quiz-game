// components/Quiz.jsx
// TODO (Step 7): Wire up Context + useReducer for quiz state.
// TODO (Step 11): Render question.imageUrl above the text when present.
export default function Quiz() {
  return (
    <section>
      <h1 className="text-2xl font-semibold mb-4">Quiz</h1>
      <p>TODO: quiz flow — fetch /quiz/start, walk through questions, submit.</p>
    </section>
  );
}
