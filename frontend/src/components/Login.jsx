// components/Login.jsx
// TODO (Step 4): Implement with React Hook Form + Zod, POST /auth/login, store token.
export default function Login() {
  return (
    <section>
      <h1 className="text-2xl font-semibold mb-4">Log in</h1>
      <p>TODO: login form (React Hook Form + Zod).</p>
    </section>
  );
}
