// pages/Admin.jsx
// TODO (Step 10): Protected route, question CRUD table, bulk import textarea.
export default function Admin() {
  return (
    <section>
      <h1 className="text-2xl font-semibold mb-4">Admin</h1>
      <p>TODO: question CRUD + bulk import (admin-only).</p>
    </section>
  );
}
